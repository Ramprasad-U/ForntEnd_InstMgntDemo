import { Attendancedetails } from './attendancedetails.model';

describe('Attendancedetails', () => {
  it('should create an instance', () => {
    expect(new Attendancedetails()).toBeTruthy();
  });
});

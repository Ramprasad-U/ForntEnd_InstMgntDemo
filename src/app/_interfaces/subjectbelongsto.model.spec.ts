import { Subjectbelongsto } from './subjectbelongsto.model';

describe('Subjectbelongsto', () => {
  it('should create an instance', () => {
    expect(new Subjectbelongsto()).toBeTruthy();
  });
});
